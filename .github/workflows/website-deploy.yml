name: website-deploy
run-name: deploy website (manual)

on:
  workflow_dispatch:
    inputs:
      broker_password:
        description: 'Override BROKER_PAGE_PASSWORD (leave blank to use code default)'
        required: false
        type: string
        default: ''

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

permissions:
  contents: read

jobs:
  deploy-website:
    name: deploy website to Fyra
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Fyra CLI
        # fyra.sh's single authoritative NS (bedok.ns.thelittlehost.com) is at
        # times unreachable from Azure eastus — runner DNS AND US DoH edges
        # both fail while other regions resolve fine. Pin via getent → DoH
        # (Cloudflare, then Google, both queried by IP) → known-IP fallback.
        # The old `curl | sh` also masked failures — download, run, verify.
        run: |
          set -euo pipefail
          pin_host() {
            host="$1"; fallback="$2"
            if getent hosts "$host" >/dev/null; then echo "$host: runner DNS ok"; return 0; fi
            ip=""
            for doh in "https://1.1.1.1/dns-query?name=${host}&type=A" \
                       "https://8.8.8.8/resolve?name=${host}&type=A"; do
              ip=$(curl -fsS --max-time 10 -H 'accept: application/dns-json' "$doh" \
                | jq -r '.Answer[]? | select(.type==1) | .data' | head -1 || true)
              [ -n "$ip" ] && { echo "$host: resolved via $doh"; break; }
            done
            if [ -z "$ip" ] && [ -n "$fallback" ]; then
              ip="$fallback"; echo "$host: DoH failed, using known-IP fallback"
            fi
            [ -n "$ip" ] || { echo "$host: unresolvable and no fallback"; return 0; }
            printf '%s %s\n' "$ip" "$host" | sudo tee -a /etc/hosts >/dev/null
          }
          pin_host fyra.sh 103.107.51.64  # last verified 2026-07-21
          curl -fsSL --retry 5 --retry-all-errors --max-time 120 \
            https://fyra.sh/install.sh -o "$RUNNER_TEMP/fyra-install.sh"
          sh "$RUNNER_TEMP/fyra-install.sh"
          PATH="$HOME/.local/bin:$PATH" command -v fyra >/dev/null \
            || { echo "fyra CLI missing after install"; exit 1; }

      - name: Configure Fyra auth
        env:
          FYRA_TOKEN: ${{ secrets.FYRA_TOKEN }}
          FYRA_SERVER: ${{ secrets.FYRA_SERVER }}
        run: |
          mkdir -p "$HOME/.fyra"
          cat > "$HOME/.fyra/config.yaml" <<EOF
          server_address: ${FYRA_SERVER:-server.fyra.sh:50051}
          token: ${FYRA_TOKEN}
          EOF
          host="${FYRA_SERVER%%:*}"; host="${host:-server.fyra.sh}"
          fallback=""; [ "$host" = server.fyra.sh ] && fallback=103.107.51.51  # last verified 2026-07-21
          if ! getent hosts "$host" >/dev/null; then
            ip=""
            for doh in "https://1.1.1.1/dns-query?name=${host}&type=A" \
                       "https://8.8.8.8/resolve?name=${host}&type=A"; do
              ip=$(curl -fsS --max-time 10 -H 'accept: application/dns-json' "$doh" \
                | jq -r '.Answer[]? | select(.type==1) | .data' | head -1 || true)
              [ -n "$ip" ] && break
            done
            ip="${ip:-$fallback}"
            [ -n "$ip" ] && printf '%s %s\n' "$ip" "$host" | sudo tee -a /etc/hosts >/dev/null || true
          fi

      - name: Build website
        working-directory: website
        env:
          GITHUB_REPO: ${{ github.repository }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BROKER_PAGE_PASSWORD: ${{ inputs.broker_password }}
        run: node build.mjs

      - name: Deploy website to Fyra
        working-directory: website/out
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          script -q -e -c "fyra push" /dev/null
